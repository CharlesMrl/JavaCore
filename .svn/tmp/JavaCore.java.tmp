<<<<<<< .mine
/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package javacore;

import DataModel.*;
import java.sql.*;
import java.util.ArrayList;
import java.util.HashMap;

/**
 *
 * @author Bastien
 */
public class JavaCore {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        
        //login
        Connection con=ConnectionManager.getConnection();
        //selection partie
        int user_id=1;
       // String query = "select * from games,moves where games.id=moves.pid and (uidb=1 or uidw=1) ";
        String query = "select * from games";
        try{
            
            ArrayList<HashMap<String,String>> results = ConnectionManager.select(query);
            System.out.println(results.toString());
            Game current = new Game(results.get(0));
            
            System.out.println(current.get("uidw"));
            System.out.println(current.get("winner"));
            current.set("uidw","3");
            current.set("winner","1");
            System.out.println(current.get("uidw"));
            System.out.println(current.get("winner"));
            current.save();
            
            /*
            ConnectionManager.update(current.getTable(),current.get("id"), "uidw", 3);
            results = ConnectionManager.select(query);
            System.out.println(results.toString());
            */
            
            
        }
        catch(Exception e){
            e.printStackTrace();
        }
        
        //mise en place des pieces
        
        //Si mon tour de jouer
            //Attendre la detection d'un coup
                //Creer objet Move (pid,uid,...)
            //Verifier la legalite du Move
            //Envoi du Move vers BDD
            //Verifier si echec et mat
                //Envoi info victoire
                //Informer joueur de la victoire
        //Si tour de l'adversaire
            //Attendre un nouveau coup
                //Effectuer deplacements du coup
                //Si coup gagnant
                    //Informer joueur de la defaite
        //Tester coherence du plateau
        
    }
    
=======
/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package javacore;

import DataModel.*;
import java.sql.*;
import java.util.ArrayList;
import java.util.HashMap;

/**
 *
 * @author Bastien
 */
public class JavaCore {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        
        //login
        Connection con=ConnectionManager.getConnection();
        int user_id=1;
        //selection partie
        try{
            ArrayList game_list = Game.find("uidw", user_id);
            
            Game current = (Game)game_list.get(0);
            current.set("winner", current.get("uidb"));
            current.print();
            current = Game.findById(1);
            current.print();
            if(current.isOver()) System.out.println("Partie finie!");
        }
        catch(Exception e){
            e.printStackTrace();
        }
        
        //mise en place des pieces
        
        //Si mon tour de jouer
            //Attendre la detection d'un coup
            //Verifier legalite
            //Envoi des infos du coup
            //Verifier si echec et mat
                //Envoi info victoire
                //Informer joueur de la victoire
        //Si tour de l'adversaire
            //Attendre un nouveau coup
                //Effectuer deplacements du coup
                //Si coup gagnant
                    //Informer joueur de la defaite
        //Tester coherence du plateau
        
    }
    
>>>>>>> .r36
}